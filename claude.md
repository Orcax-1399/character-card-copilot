# 本项目采用技术栈
包管理器: pnpm
UI框架: tailwindcss 4.0 (已经配置完成)
pinia (已经安装)
vue-router (已经安装)
应用架构: tauri: vue前端 + rust后端
使用 vue-icons-plus 作为图标支持(已经配置完全)
```typescript
import { IconName } from 'vue-icons-plus/md'
```

# 开发要求
专注于单个组件的开发，不要一次做过多的开发，完全遵守用户要求
使用Typescript，同时避免使用any

# 项目介绍
角色卡编辑器 Character-Card-Copilot
使用AI进行辅助，可视化的编辑角色卡

# 组件要求
按钮: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full"
